services:
  spigot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: spigot-mcp-test
    ports:
      - "25565:25565"
      - "25575:25575"
      - "8080:8080"
    volumes:
      - ./spigot-data:/data
    restart: unless-stopped
    tty: true
    stdin_open: true